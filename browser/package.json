{
  "name": "example-app-browser",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist; cp src/index.html dist/; npx esbuild --bundle --format=esm --keep-names --platform=browser --outdir=dist src/app.js src/worker.js",
    "build:watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@stevvvns/incomponent": "^0.0.15",
    "@stevvvns/koa-wsapi": "^0.0.9",
    "tweetnacl": "^1.0.3"
  }
}
